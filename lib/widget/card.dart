import 'package:flutter/material.dart';

class OverflowCard extends StatelessWidget {
  const OverflowCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}